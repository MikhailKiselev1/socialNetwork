CREATE TABLE friendship_status (
                                   id serial PRIMARY KEY,
                                   time TIMESTAMP NOT NULL,
                                   name VARCHAR (255),
                                   code VARCHAR (255)
);